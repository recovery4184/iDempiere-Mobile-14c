version: 2.1
jobs:
  build:
    docker:
      - image: circleci/android:api-27
    steps:
      - checkout
      - run:
          name: Download APK
          command: |
            curl -L -o 14c_app-release.apk "https://median.co/share/jbwqadm#apk"
      - run:
          name: Upload to Appetize.io
          command: |
            curl https://api.appetize.io/v1/apps \
              -F "file=@14c_app-release.apk" \
              -F "platform=android" \
              -F "token=tok_xanhdvlpfbeovwj55kgsridauu"
workflows:
  version: 2
  build_and_upload:
    jobs:
      - build